# Documentation index

Read the root `README.md` for setup and `PRODUCT.md` for scope.
`ARCHITECTURE.md` and `DESIGN.md` describe the implementation and UX;
`LLM-PROVIDERS.md` and `PRIVACY.md` cover integrations and data handling.
Historical decisions and known issues remain in `MISC.md`, `PITFALLS.md`, and
`CHANGELOG.md`.
