[package]
name = "solum-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

description = "Solum command-line interface — exercises the solum-core closed loop headlessly."

[[bin]]
name = "solum"
path = "src/main.rs"

[dependencies]
solum-core = { path = "../solum-core" }
chrono = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
clap = { workspace = true }
