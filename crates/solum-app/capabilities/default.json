{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs + OS notifications; all real work goes through app commands.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
